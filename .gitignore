# macOS noise
.DS_Store

# dependencies
node_modules/

# logs / temp
*.log
tmp/
.cache/

# common secret files (keep your own patterns here)
.env
.env.*
*secret*
*secrets*
*.key
*.pem

# optional: if these are not useful to version
# bun.lock
# package-lock.json
# pnpm-lock.yaml

# Ignore opencode runtime/config files at the repo root.
opencode/*
!opencode/opencode.json
!opencode/agent/
!opencode/agent/**
!opencode/skills/
!opencode/skills/**
